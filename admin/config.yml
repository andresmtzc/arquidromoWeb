backend:
  name: github
  repo: andresmtzc/arquidromoWeb
  branch: main
  auth_scope: repo
  base_url: https://auth.netlify.com

media_folder: ""
public_folder: ""

collections:
  - name: "lotstatus"
    label: "Lot Status"
    files:
      - file: "lot-status.json"
        label: "Lot Data"
        name: "lots"
        editor:
          preview: false
        fields:
          - label: "Lot List"
            name: "lots"
            widget: list
            summary: "Lot {{fields.id}} - {{fields.status}}"
            fields:
              - { label: "ID", name: "id", widget: number }
              - { label: "Status", name: "status", widget: select, options: ["available", "sold", "unavail"] }
